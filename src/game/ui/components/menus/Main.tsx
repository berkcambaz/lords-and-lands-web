import { Soda } from "@dorkodu/soda";

export function Main() {
  return (
    <div class="__menu">
      <button class="__main-btn" onclick={() => { }}>New</button>
      <button class="__main-btn" onclick={() => { }}>Load</button>
      <button class="__main-btn" onclick={() => { }}>Save</button>
      <button class="__main-btn" onclick={() => { }}>Settings</button>
    </div>
  )
}